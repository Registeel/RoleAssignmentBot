const http = require("../utils/http.js")

/**
 * The class for settings up bindings, will do everything for you that has to do with posting statistics.
 */
module.exports = class Bindings {

	/**
	 * @param [BindingOptions] [options]
	 */
	constructor(options) {

	}

  /**
   * Binds the client to the bindings manager
   * @returns {Promise}
   */
	bind(client) {
		// still in development
	}

}